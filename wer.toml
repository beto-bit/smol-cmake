[build]
dir = "build"
generator = "Ninja"

[unix]
cc = "clang"
cxx = "clang++"
generator = "Unix Makefiles"

[format]
glob = ["src/**/*.cpp", "src/**/*.hpp", "include/**/*.hpp"]

[vcpkg]
enable = true
